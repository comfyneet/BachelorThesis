<html>
<head>
    <link href="~/css/csstemp.css" rel="stylesheet" />
</head>
<body>
@if (ViewBag.co == '0')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>
                @foreach (var a in ViewBag.allA)
                {
                    <li >
                        @{ String s = "/Ontology/ShowRelation?id=" + @a.Id; }
                        <a href=@s onclick="clickMe();">@a.Id</a>
                    </li>
                }                
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>
}

@if (ViewBag.co == '1')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>
                @foreach (var a in ViewBag.allA)
                {
                    <li>
                        @{ String s = "/Ontology/ShowAttribultResult?id=" + @a.Id; }
                        <a href=@s onclick="clickMe();">@a.Id</a>
                    </li>
                            }
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>
 }
@if (ViewBag.co == '2')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>
                
                @foreach (var a in ViewBag.classsub)
                {
                    <li>
                        @{ String s = "/Ontology/GetDirectSubClass?id=" + @a; }
                        <a href=@s onclick="clickMe();">@a</a>
                    </li>
                  }
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>

  }
@if (ViewBag.co == '3')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>               
                @foreach (var a in ViewBag.allInvidual)
                {
                    <li>
                        @{ String s = "/Ontology/ShowInvidual?id=" + @a.Id; }
                        <a href=@s onclick="clickMe();">@a.Id</a>
                    </li>
                            }                           
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>                            }
</body>
</html>

@if (ViewBag.co == '4')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>
                @foreach (var a in ViewBag.directSubClasses)
                {
                    <li>
                        @{ String s = "/Ontology/GetDirectSubClass?id=" + @a; }
                        <a href=@s>@a</a>
                    </li>
                            }
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>

}
@if (ViewBag.co == '5')
{
    <div id="wrapper">
        <!-- Sidebar -->
        <div id="sidebar-wrapper">
            <ul class="sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">
                        Start Bootstrap
                    </a>
                </li>           
                    <li>                      
                        @{ String s = "/Ontology/GetInvidual?id=" + @ViewBag.getIn; }
                        <a href=@s > @ViewBag.getIn</a>
                    </li>
                            
            </ul>
        </div>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div class="col-md-10 col-sm-8 main-content ">
            <!--Main content code to be written here -->
        </div>
        <!-- /#page-content-wrapper -->
    </div>

  }
